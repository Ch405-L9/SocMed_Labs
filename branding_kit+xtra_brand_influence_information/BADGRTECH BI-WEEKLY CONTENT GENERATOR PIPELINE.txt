Below is the fully automated BADGRTech Daily Content Generator Pipeline v1.0, extended from your RAG + brand kit system into an end-to-end execution architecture.

This is designed so Claude Code (or any agent layer) can run it as a repeatable daily job with minimal human input.

🧠 BADGRTECH DAILY CONTENT GENERATOR PIPELINE (v1.0)
🧩 SYSTEM OVERVIEW

This pipeline converts:

trends + services + BADGR tone system
→ into platform-specific content packs
→ automatically formatted for posting or API dispatch

🏗️ 1. FULL ARCHITECTURE
                        ┌──────────────────────┐
                        │  TREND INPUT LAYER   │
                        │ (research file / API)│
                        └─────────┬────────────┘
                                  │
                                  ▼
                 ┌────────────────────────────┐
                 │ INTENT + CONTENT SELECTOR  │
                 │ (what to post today?)      │
                 └─────────┬──────────────────┘
                           │
                           ▼
        ┌────────────────────────────────────┐
        │ BADGR RAG SYSTEM (CORE MEMORY)     │
        │ tone + audience + enforcement      │
        └─────────┬──────────────────────────┘
                  │
                  ▼
     ┌──────────────────────────────────────┐
     │ CONTENT GENERATION ENGINE (CLAUDE)   │
     │ - Ad Kit                            │
     │ - Post Kit                          │
     │ - Profile Updates (optional)        │
     └─────────┬────────────────────────────┘
               │
               ▼
     ┌──────────────────────────────────────┐
     │ PLATFORM DISTRIBUTION FORMATTER      │
     │ TikTok / IG / X / YT / FB outputs    │
     └─────────┬────────────────────────────┘
               │
               ▼
     ┌──────────────────────────────────────┐
     │ OUTPUT SINK                         │
     │ - JSON files                        │
     │ - Copy/paste packs                  │
     │ - API-ready payloads               │
     └──────────────────────────────────────┘
⚙️ 2. DAILY EXECUTION PIPELINE (STEP LOGIC)
STEP 1 — TREND INGESTION

Inputs:

trend_research.json
past high-performing posts
competitor signals (optional)

Output:

{
  "trend_topics": [],
  "viral_patterns": [],
  "relevant_to_badgr_services": []
}
STEP 2 — CONTENT SELECTION ENGINE

Selects:

1–2 service-based posts
1 educational/tutorial post
1 short-form viral adaptation

Rules:

must map to BADGR services or engineering niche
must pass enforcement rules
must align with tone system

Output:

{
  "selected_content": [
    "web optimization service ad",
    "AI automation breakdown tutorial",
    "short viral hook adaptation"
  ]
}
STEP 3 — TONE + AUDIENCE ENFORCEMENT LAYER

Always inject:

BADGRTECH_TONE_SYSTEM
audience mode (auto-detected or default business_client)
enforcement rules

This layer ensures:

no output is generated outside brand voice

STEP 4 — CONTENT GENERATION ENGINE

Generates:

A. AD KIT
service ads
app promotion
lead gen copy
B. POST KIT
tutorial content
breakdowns
educational threads
C. PROFILE KIT (optional daily rotation check)
bio optimization
pinned post updates
CTA refresh
STEP 5 — PLATFORM ADAPTATION ENGINE

Transforms each output:

TikTok
hook-first
ultra compressed
Instagram
readable blocks
slightly expressive
X (Twitter)
insight punch format
YouTube
structured long-form script + shorts variant
Facebook
slightly extended clarity version
STEP 6 — FINAL OUTPUT PACKAGER

Output structure:

{
  "date": "",
  "brand": "BADGRTech",
  "daily_content_pack": {
    "ads": {
      "facebook": [],
      "instagram": [],
      "tiktok": [],
      "x": [],
      "youtube": []
    },
    "posts": {
      "youtube_long": [],
      "short_form_variants": []
    },
    "profiles": {
      "updated_profiles": []
    }
  },
  "tone_applied": "BADGRTECH_TONE",
  "enforcement_pass": true
}
🔁 3. AUTOMATION LOOP (DAILY RUN)
FOR each day:

1. Load trend_research.json
2. Run content selection engine
3. Apply BADGR tone + enforcement
4. Generate all kits
5. Adapt per platform
6. Export JSON + copy-ready blocks
7. Store in /brand/social/output/YYYY-MM-DD/
🧠 4. CLAUDE CODE MASTER EXECUTION PROMPT

This is what you give Claude Code directly:

You are executing the BADGRTECH DAILY CONTENT GENERATION PIPELINE.

You MUST:

1. Load BADGRTECH_TONE_SYSTEM
2. Load enforcement policy (highest priority)
3. Load audience adaptation module
4. Load message framework
5. If available, load trend_research.json

---

TASK:
Generate a full daily content pack including:

- Ad Kits
- Post Kits
- Short-form adaptations
- Platform-specific variants

for:
Facebook, Instagram, TikTok, X (Twitter), YouTube

---

RULES:
- NO generic marketing language
- NO fluff or corporate tone
- MUST be BADGRTECH tone compliant
- MUST be copy-paste ready
- MUST be structured JSON output
- MUST include platform adaptation differences

---

OUTPUT FORMAT:
Strict JSON only:

{
  "daily_pack": {...},
  "tone_applied": "BADGRTECH_TONE",
  "enforcement_status": "PASS",
  "notes": ""
}

---

If trend data is provided:
- extract only structural patterns
- do NOT copy content directly
- adapt hooks only

END
📊 5. OPTIONAL ADD-ON: VIRAL PATTERN ENGINE (HIGH VALUE UPGRADE)

If you want next level automation:

Add:

- hook pattern detection
- retention curve modeling
- engagement probability scoring
- platform-specific virality adjustment

This allows:

“generate content that statistically matches viral structures”

⚠️ 6. DESIGN REALITY CHECK (IMPORTANT)

What you now have is:

NOT a content generator

It is:

a constrained autonomous content production system with deterministic brand enforcement + platform routing

Key properties:

repeatable daily execution
no tone drift
modular expansion
RAG-driven behavior control
